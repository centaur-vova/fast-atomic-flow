# Clients (4222)
port: 4222

# Monitoring (http)
http_port: 8222

jetstream {
    store_dir: "/data"
    max_memory_store: 64M
    max_file_store: 1G # RAM only for messages, some space left for config data
}

authorization {
  token: $NATS_TOKEN
}
