# Clients' port (4222 by default)
port: $NATS_PORT

jetstream {
    store_dir: "/data"
    max_memory_store: 64M
    max_file_store: 1G # RAM only for messages, some space left for config data
}

authorization {
  token: $NATS_TOKEN
}
